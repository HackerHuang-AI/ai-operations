server.port=8060
app.environment=dev

logging.file.path=logs
logging.level.com.ai.operations=DEBUG

spring.cloud.nacos.config.server-addr=127.0.0.1:8848
spring.cloud.nacos.discovery.server-addr=127.0.0.1:8848
dubbo.registry.register=false

# ==================== Vault（本地 AppRole + 静态密钥）====================
# 启动阶段导入 Vault 配置。
spring.config.import=optional:vault://,optional:nacos:ai-operations.properties
# 启用 Vault 配置中心。
spring.cloud.vault.enabled=true
# Vault 服务地址。
spring.cloud.vault.host=127.0.0.1
spring.cloud.vault.port=8200
spring.cloud.vault.scheme=http
# 使用 AppRole 认证方式。
spring.cloud.vault.authentication=APPROLE
# Local 默认凭据仅用于本地开发；实际投产必须由运行环境注入 VAULT_ROLE_ID 和 VAULT_SECRET_ID，禁止保留明文。
spring.cloud.vault.app-role.role-id=${VAULT_ROLE_ID:108e2ea8-a1eb-fc76-ebd0-82aed1ed609a}
spring.cloud.vault.app-role.secret-id=${VAULT_SECRET_ID:5d4b5736-6175-78ae-11b3-4cd2c31bcc11}
# AppRole 认证挂载路径。
spring.cloud.vault.app-role.app-role-path=approle
# KV v2 路径规则：secret/<service>/<profile>。
spring.cloud.vault.kv.backend=secret
spring.cloud.vault.kv.default-context=
spring.cloud.vault.kv.application-name=${spring.application.name}/${spring.profiles.active}
spring.cloud.vault.kv.profiles=

# ==================== Redis Cluster（凭据由 Vault 注入，DB 0 + Key 前缀隔离）====================
spring.data.redis.cluster.nodes=127.0.0.1:7001,127.0.0.1:7002,127.0.0.1:7003
spring.data.redis.lettuce.cluster.refresh.adaptive=true
spring.cache.redis.key-prefix=ai-operations:
spring.cache.redis.use-key-prefix=true

# ==================== 数据库（经 HAProxy 统一入口）====================
spring.datasource.url=jdbc:mysql://127.0.0.1:6033/ai_operations?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

